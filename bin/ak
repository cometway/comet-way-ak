#! /bin/sh

# This is a shell script for starting the Comet Way Agent Kernel.
# You may need to change the classpath if you are using Java 1.x
# or have installed the Agent Kernel into a different location.

CLASSPATH=~/ak/classes:~/ak/ak.jar
java -classpath ${CLASSPATH} com.cometway.ak.AK $*
